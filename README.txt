atproxy is a transparent proxy that can be used to redirect an
Android app's TCP traffic via an upstream HTTP proxy. Routes
traffic from a specific Android UID through an upstream HTTP
proxy (e.g. Burp Suite) using iptables NAT REDIRECT.

 Android      ...........       Upstream 
   app    <-> | atproxy | <->  HTTP Proxy  <->  Original dest.
(uid: 37)     ``````````'    (pentest:8080)

Example commands:
./atproxy 37 pentest:8080
./atproxy com.example pentest:8080

Pre-built binaries: https://github.com/lc-at/atproxy/releases

License: MIT

Note: This software is mostly generated by AI (GLM-5.1). 
